{
  "scripts": {
    "prestart": "targz python_overrides python-overrides.tgz",
    "start": "browser-sync start --server . --files . --single --config ./bs-config.js"
  },
  "dependencies": {
    "browser-sync": "^3.0.3",
    "targz": "1.0.1"
  }
}
